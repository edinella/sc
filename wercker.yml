box: node
build:
  steps:
    - script:
      name: print node version
      code: |
        echo "node version $(node -v) running"
        echo "npm version $(npm -v) running"
    - npm-install
    - script:
      name: generate pages
      code: |
        npm run generate
deploy:
  steps:
    - script:
        name: push to Surge.sh
        code: ./node_modules/surge/bin/surge ./public --token $SURGE_TOKEN
